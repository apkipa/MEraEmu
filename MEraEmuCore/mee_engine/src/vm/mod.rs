mod inst;

// TODO: Will handle a bytecode instruction `InvokeRegisteredHandler` to call
//       user-provided handlers registered into the engine. User will need to
//       provider a handler function and a handler code (unique).

