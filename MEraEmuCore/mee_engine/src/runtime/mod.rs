// TODO: Define `EraRuntime` which is the previous `EraCompilerContext`. Parsers etc. will
//       no longer depend on `EraRuntime` directly, instead the inner split contexts.

pub struct EraRuntime {
    // pub codegen: crate::codegen::CodegenContext,
    // pub ast: crate::ast::AstContext,
    // pub interpret: crate::ast::interpret::InterpretContext,
}
